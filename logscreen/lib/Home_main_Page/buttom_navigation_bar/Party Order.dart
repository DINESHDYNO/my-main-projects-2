import 'package:flutter/material.dart';


class PartyOrder extends StatelessWidget {
  const PartyOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PartyOrder'),),
    );
  }
}
